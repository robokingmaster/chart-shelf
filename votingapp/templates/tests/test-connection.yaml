apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "votingapp.fullname" . }}-test-connection"
  labels:
    {{- include "votingapp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "votingapp.fullname" . }}:{{ .Values.vote.port }}']
  restartPolicy: Never
