apiVersion: v1
kind: Service
metadata:
  name: vote
spec:
  selector:
    app: vote
  ports:
    - protocol: TCP
      port: 8080
      targetPort: {{ .Values.vote.port }}
      nodePort: {{ .Values.vote.nodePort }}
  type: {{ .Values.vote.service.type }}
